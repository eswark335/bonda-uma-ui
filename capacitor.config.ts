import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bondauma.app',
  appName: 'bonda-uma-ui',
  webDir: 'dist/bonda-uma-ui/browser'
};

export default config;
